<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:l="sap.ui.layout"
    xmlns:mvc="sap.ui.core.mvc">
    <HBox justifyContent="SpaceBetween" class="footer-hbox desktop-view-footer">
        <HBox class="sapUiSmallMarginTopBottom f-img-wrap">
            <Image class="fotterImage" src="{path: 'view>/welcomeLogoVertical',
                        formatter : '.formatter.pictureUrl'
                        }" press="onNavBackHome">
            </Image>
        </HBox>
        <VBox class="fotterLink">
            <Text text="{i18n>FooterInfo}" class="sapUiSmallMarginTopBottom"/>
            <Link text="{i18n>aboutUs}" press="onPressImprint"/>
            <Link text="{i18n>contact}" press="onPressImprint"/>
            <Link text="{i18n>imprint}" press="onPressImprint"/>
            <Link text="{i18n>Terms}" press="onPressImprint"/>
            <Link text="{i18n>Privacy}" press="onPressImprint"/>
            <Link text="{i18n>GDPR}" press="onPressImprint"/>
        </VBox>
        <VBox class="fotterAvatar">
            <Text text="{i18n>FooterSoc}" class="sapUiSmallMarginTopBottom"/>
            <Avatar displaySize="XS" showBorder="true" press="onPressFaceBook" class="logo" src="{path: 'view>/facebook',
                        formatter : '.formatter.pictureUrl'
                        }" />
            <!--<Avatar displaySize="XS" showBorder="true" press="onPressFaceBook" class="logo" src="{path: 'view>/pinterest',
                        formatter : '.formatter.pictureUrl'
                        }" />-->
            <Avatar displaySize="XS" showBorder="true" press="onPressInstaGram" class="logo" src="{path: 'view>/instagram',
                        formatter : '.formatter.pictureUrl'
                        }" />
            <Avatar displaySize="XS" showBorder="true" press="onPressYouTube" class="logo" src="{path: 'view>/youtube',
                        formatter : '.formatter.pictureUrl'
                        }" />
        </VBox>
        <VBox>
            <Text text="{i18n>FooterCat}" class="sapUiSmallMarginTopBottom"/>
            <VBox class="fotterLink" items="{
                        path: 'oDataCategory>/',
                        filters: [{path: 'parent', operator: 'EQ', value1: 'None'}],
                        sorter: {path: 'id'}
                    }">
                <Link text="{oDataCategory>category_name}" press="onCategoryLinkPress"/>
            </VBox>
        </VBox>
    </HBox>
</core:FragmentDefinition>